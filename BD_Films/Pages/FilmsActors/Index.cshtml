@page
@model BD_Films.Pages.FilmsActors.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FilmsActor[0].NameHero)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilmsActor[0].IdActorNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FilmsActor[0].IdFilmNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.FilmsActor) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameHero)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdActorNavigation.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdFilmNavigation.Id)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
